<div class="feature-image-left-section tailwind" {{ block.shopify_attributes }} id="{{ section.settings.scrollID }}">
  <!-- App Feature:Image Left -->
  <div class="mt-16 max-w-6xl px-4 py-10 mx-auto my-6 sm:px-6 lg:px-8 lg:py-14 lg:my-0 {% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
    <!-- Grid -->
    <div class="flex flex-col-reverse items-center md:grid md:grid-cols-2 md:items-center md:gap-12 xl:gap-32">
      <div>
        {%- if section.settings.feature_image_left_image != blank -%}
          {%- assign widths = '165, 360, 535, 750, 1070, 1500' -%}
          {%- capture sizes -%}
                  (min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 100 | divided_by: 2 }}px,
                  (min-width: 750px) calc((100vw - 130px) / 2), calc((100vw - 50px) / 2)
              {%- endcapture -%}
          {{
            section.settings.feature_image_left_image
            | image_url: width: 907, height: 1738
            | image_tag:
              loading: 'lazy',
              sizes: sizes,
              widths: widths,
              alt: section.settings.feature_image_left_image_alt,
              class: 'md:-ml-20 rounded-xl md:max-w-[40rem] lg:max-w-md mt-8 lg:mt-0'
            | escape
          }}
        {%- else -%}
          <img
            class="-ml-20 rounded-xl max-w-[40rem] lg:max-w-md mt-8 lg:mt-0"
            src="{{ 'feature_image_left_placeholder.png' | asset_url }}"
            alt="Placeholder Image"
            loading="lazy"
            width="656"
            height="496"
          >
        {%- endif -%}
      </div>
      <!-- End Col -->

      <div class="max-w-[40rem] lg:max-w-none">
        <div class="space-y-6 sm:space-y-8">
          <!-- Title -->
          <div class="-mt-20 space-y-2 md:space-y-4">
            <h2 class="text-3xl font-bold text-gray-800 lg:text-4xl dark:text-gray-200">
              {{ section.settings.feature_image_left_title }}
            </h2>
            <p class="text-gray-500 max-w-[44rem]">
              {{ section.settings.feature_image_left_description }}
            </p>
            <!-- CTA: OPEN MODAL -->
            <button
              id="cta_button__feature__open_modal__{{ section.id }}"
              type="button"
              class="{{ section.settings.feature_image_left_cta_modal_status }} inline-flex cursor-pointer justify-center items-center gap-x-3.5 text-sm lg:text-base text-center border hover:border-gray-300 hover:scale-105 shadow-sm font-medium rounded-3xl focus:outline-none focus:ring-2 focus:ring-gray-400 focus:ring-offset-2 focus:ring-offset-white transition py-3 px-4 dark:border-gray-800 dark:hover:border-gray-600 dark:shadow-slate-700/[.7] dark:text-white dark:focus:ring-gray-700 dark:focus:ring-offset-gray-800"
              data-hs-overlay="#{{ section.settings.feature_image_left_cta_modal_target }}"
            >
              {{ section.settings.feature_image_left_cta_modal_label }}
            </button>
          </div>
          <!-- End Title -->
        </div>
      </div>
      <!-- End Col -->
    </div>
    <!-- End Grid -->
  </div>

  <!-- End  App Feature:Image Left ) -->
</div>

{% schema %}
{
  "name": "Feature - Image Left",
  "settings": [
    {
      "id": "scrollID",
      "type": "text",
      "label": "Smooth Scroll Link ID",
      "default": "feature-1"
    },
    {
      "id": "feature_image_left_title",
      "type": "text",
      "label": "Title",
      "default": "Feature X."
    },
    {
      "id": "feature_image_left_description",
      "type": "text",
      "label": "Description",
      "default": "Lorem ipsum dolor sit amet, consetetur sadipscing elitr, sed diam non. Lorem ipsum dolor sit amet, consetetur sadipscing elitr, sed diam non.Lorem ipsum dolor sit amet, consetetur sadipscing elitr, sed diam non."
    },
    {
      "id": "feature_image_left_image",
      "type": "image_picker",
      "label": "Image"
    },
    {
      "id": "feature_image_left_image_alt",
      "type": "text",
      "label": "Image Alt Text",
      "default": "Lorem ipsum dolor sit amet, consetetur sadipscing elitr, sed diam non."
    },
    {
      "id": "feature_image_left_cta_modal_status",
      "type": "radio",
      "label": "Show Link To Modal Dialog?",
      "options": [
        {
          "value": "show_cta_to_modal",
          "label": "Yes"
        },
        {
          "value": "hidden",
          "label": "No"
        }
      ]
    },
    {
      "id": "feature_image_left_cta_modal_label",
      "type": "text",
      "label": "Modal CTA Button",
      "default": "Learn more"
    },
    {
      "id": "feature_image_left_cta_modal_target",
      "type": "text",
      "label": "Modal CTA Target ID",
      "default": "my-modal-id"
    }
  ],
  "presets": [
    {
      "name": "Feature - Image Left",
      "category": "Custom"
    }
  ]
}
{% endschema %}
